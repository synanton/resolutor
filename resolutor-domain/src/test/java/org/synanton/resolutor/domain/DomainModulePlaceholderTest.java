package org.synanton.resolutor.domain;

import static org.assertj.core.api.Assertions.assertThat;

import org.junit.jupiter.api.Test;

/** Placeholder test so the domain module has a non-empty test task in phase 0. */
class DomainModulePlaceholderTest {

  @Test
  void moduleCompilesAndTestsRun() {
    assertThat("resolutor-domain").isNotBlank();
  }
}
